MONGO_URI=mongodb://localhost:27017/hackathon-july
PORT=4000
# Optional: if set, gates the whole API behind a single shared password (POST /api/login).
# Leave unset to disable the login gate entirely.
APP_PASSWORD=
